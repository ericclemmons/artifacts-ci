# Based on https://viteplus.dev/guide/ci#github-actions
name: CI

on:
  pull_request:
  push:

jobs:
  check:
    runs-on: ubuntu-latest
    env:
      npm_config_store_dir: /workspace/.cache/pnpm-store
      XDG_CACHE_HOME: /workspace/.cache
    steps:
      - uses: actions/checkout@v4
      - uses: voidzero-dev/setup-vp@v1
        with:
          node-version: "22"
          cache: true
      - run: vp install
      - run: vp check
      - run: vp test
      - run: vp build
