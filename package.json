{
  "name": "artifacts-ci",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vp run -r build",
    "check": "vp fmt && vp run -r lint && vp run -r test && vp run -r build",
    "dev": "pnpm --parallel --filter docker-proxy --filter ci --filter git dev",
    "dev:act": "pnpm --parallel --filter docker-proxy --filter ci --filter git dev:act",
    "dev:agent-ci": "pnpm --parallel --filter docker-proxy --filter ci --filter git dev:agent-ci",
    "dev:local-actions": "pnpm --parallel --filter docker-proxy --filter ci --filter git dev:local-actions",
    "dev:workflows": "pnpm --parallel --filter docker-proxy --filter ci --filter git dev:workflows",
    "lint": "vp run -r lint",
    "prepare": "vp config",
    "smoke": "./scripts/smoke-test.sh",
    "test": "vp run -r test"
  },
  "devDependencies": {
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@11.1.2"
}
